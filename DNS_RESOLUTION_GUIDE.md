# QuestEd DNS問題解決ガイド

## 現状の問題

### 症状
- **ドメインアクセス**: `https://quest-ed.jp` → 502 Bad Gateway (nginx/1.26.3)
- **IPアクセス**: `https://3.115.238.137` → 正常稼働中 (Apache + PHP)

### 根本原因
DNS設定が古いサーバーを指しているため：
- **現在のDNS**: `quest-ed.jp` → `54.95.225.241`, `3.114.222.14` (nginx, 502エラー)
- **正しいサーバー**: `3.115.238.137` (Apache, 正常稼働)

---

## 解決方法1: DNS Aレコード変更（推奨）

### AWS Route53の場合

#### ステップ1: AWS Management Consoleにログイン
1. https://aws.amazon.com/console/ にアクセス
2. Route53サービスを選択

#### ステップ2: ホストゾーン選択
1. "Hosted zones" をクリック
2. `quest-ed.jp` ドメインを選択

#### ステップ3: Aレコード変更
1. Type "A" のレコードを見つける
2. "Edit" ボタンをクリック
3. **Value** を変更:
   - **変更前**: `54.95.225.241` または `3.114.222.14`
   - **変更後**: `3.115.238.137`
4. "Save changes" をクリック

#### ステップ4: 変更確認（5-10分後）
```bash
# DNS変更確認コマンド
nslookup quest-ed.jp
# または
dig quest-ed.jp

# 期待する結果: 3.115.238.137 が返される
```

### 他のDNSプロバイダーの場合

#### お名前.com
1. お名前.com Naviにログイン
2. "DNS設定" → "DNSレコード設定"
3. Aレコードを `3.115.238.137` に変更

#### Cloudflare
1. Cloudflareダッシュボードにログイン
2. ドメインを選択 → "DNS" タブ
3. Aレコードを編集して `3.115.238.137` に変更

#### ムームードメイン
1. ムームードメインコントロールパネルにログイン
2. "ドメイン設定" → "DNS設定"
3. Aレコードを `3.115.238.137` に変更

---

## 解決方法2: 暫定対応（即座に可能）

DNS変更の伝播を待つ間、以下の方法でユーザーに案内：

### A. IPアドレス直接アクセスの案内
**正常アクセスURL**: `https://3.115.238.137`

### B. ユーザー向け案内文
```
【重要なお知らせ】QuestEd学習システムのアクセス方法について

現在、ドメイン設定の調整中のため、以下のURLからアクセスしてください：

🔗 QuestEd学習システム: https://3.115.238.137

※ 通常のquest-ed.jpドメインは近日中に復旧予定です
※ 学習データやアカウント情報に影響はありません
※ ご不便をおかけし申し訳ありません

問題解決まで引き続き上記URLをご利用ください。
```

---

## 検証手順

### DNS変更後の確認方法

#### 1. コマンドラインでの確認
```bash
# DNS解決確認
nslookup quest-ed.jp

# HTTPアクセス確認  
curl -I https://quest-ed.jp

# 期待する結果:
# - nslookup: 3.115.238.137 が返される
# - curl: HTTP/2 200 (Apache応答)
```

#### 2. ブラウザでの確認
1. `https://quest-ed.jp` にアクセス
2. 正常にQuestEdのログインページが表示される
3. 502 Bad Gatewayエラーが表示されない

#### 3. 複数地点での確認
- https://www.whatsmydns.net/ で world-wide DNS伝播状況を確認
- 異なるネットワークからのアクセステスト

---

## DNS伝播タイミング

### 一般的な伝播時間
- **最短**: 5-15分（キャッシュが少ない場合）
- **通常**: 1-4時間（一般的なケース）  
- **最長**: 24-48時間（最大TTL期間）

### 伝播を早める方法
1. **TTL値を事前に短縮**: 変更前にTTLを300秒（5分）に設定
2. **DNSフラッシュ**: `ipconfig /flushdns` (Windows) または `sudo dscacheutil -flushcache` (Mac)
3. **公開DNSサーバー確認**: 8.8.8.8, 1.1.1.1 での解決確認

---

## トラブルシューティング

### 問題: DNS変更が反映されない
**対策**:
1. TTL期間の確認（変更前のTTL時間だけ待つ必要）
2. 複数のDNSサーバーでの確認
3. ブラウザキャッシュのクリア

### 問題: 証明書エラーが発生
**対策**:
1. SSL証明書がIPアドレス対応かドメイン対応かの確認
2. 必要に応じて証明書の再発行または設定変更

### 問題: 一部のユーザーだけアクセスできない
**対策**:
1. 地域的なDNS伝播の遅延
2. ISP固有のDNSキャッシュ問題
3. 時間をおいて再確認

---

## 成功基準

✅ **DNS解決**: `nslookup quest-ed.jp` が `3.115.238.137` を返す
✅ **HTTPアクセス**: `curl -I https://quest-ed.jp` が HTTP/2 200を返す  
✅ **Webアクセス**: ブラウザで正常にQuestEdが表示される
✅ **継続安定性**: 24時間継続して正常アクセス可能

---

## 作業完了後の確認事項

1. **ユーザー案内の更新**: 暫定案内の撤去
2. **監視の継続**: 数日間のアクセス状況監視
3. **バックアップ計画**: 今後の同様問題に備えた対策策定
4. **ドキュメント更新**: 今回の問題と解決方法の記録

---

**作成日**: 2025年8月8日
**対象システム**: QuestEd Ver.1.4
**問題種別**: DNS設定不一致による502 Bad Gateway
**解決予想時間**: DNS変更5分 + 伝播待ち1-24時間